import { BindTemplateDirective } from './bind-template.directive';

describe('BindTemplateDirective', () => {
	it('should create an instance', () => {
		const directive = new BindTemplateDirective();
		expect(directive).toBeTruthy();
	});
});
