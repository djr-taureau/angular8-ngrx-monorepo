import { InlineCaretDirective } from './inline-caret.directive';

describe('InlineCarryDirective', () => {
	it('should create an instance', () => {
		const directive = new InlineCaretDirective(null, null);
		expect(directive).toBeTruthy();
	});
});
